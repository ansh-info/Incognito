# Use your own credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=incognito
DB_PORT=3306

# For Docker environment
# Uncomment the below line when running inside Docker
# DB_HOST=mysql     #mysql_container_name  # Replace with your actual Docker container name